{
  "name": "paperasse",
  "version": "0.1.0-lot0",
  "private": true,
  "description": "Paperasse — Module comptabilité française intégré à Serenity",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
